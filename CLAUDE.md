# Claude Code Rules

このファイルは、このプロジェクトでClaude Codeを使用する際のルールを定義します。

## Language Guidelines
- 思考は英語で行う
- 会話は日本語で行う

## Git運用ルール

### コミットメッセージ
- createdBy claude~の文言は不要
- fix:,feat:などのプレフィックスを入れる

### コミット前の承認
- **各タスクが完了したら、gitにcommitする前に必ずユーザーの承認を求めること**
- 承認なしに自動的にcommitしないこと
- 変更内容を明確に説明し、ユーザーが確認できるようにすること

## Test-Driven Development (TDD)

- 原則としてテスト駆動開発（TDD）で進める
- 期待される入出力に基づき、まずテストを作成する
- 実装コードは書かず、テストのみを用意する
- テストを実行し、失敗を確認する
- その後、テストをパスさせる実装を進める
- 実装中はテストを変更せず、コードを修正し続ける
- すべてのテストが通過するまで繰り返す
- mockitoを用いて行う
- UnitTest,WidgetTest,IntegrationTestを実施


## プロジェクトについて
## 1. プロジェクト概要
- **What**: 筋トレのメニューをtodoリストで管理して記録するApp
- **Why**: 手軽に使えてYoutubeをアプリ内で再生できるようなAppが欲しいから
- **Who**: 筋トレする人(特にYoutubeを観ながら)

## 2. 主要機能
- 筋トレメニューの日ごとにTodoリストを作成、編集、削除できる
- 作成したTodoリストに対して「完了」状態に変更することができる(チェックをつけるのが快感な感じになるように)
- 任意のYoutubeをApp内で再生できる
- 筋トレ時間計測
- 筋トレ履歴確認

## 3. ユーザーの実際の使用の流れ
- 筋トレ開始ボタン押下
- ＞タイマーカウントアップ開始
- ＞任意のYoutubeをApp内で再生開始
- ＞完了したメニューを随時チェックする
- ＞筋トレ終了ボタン押下
- ＞筋トレ記録としてローカルに保存

# 4. 使用ライブラリやデザインパターン
- 基本的にパッケージは最新のバージョンを使用する(ライブラリ同士がコンフリクトする場合は変更可)
- flutter_riverpod+MVVMでのデザインパターンを使用
- riverpodを用いているのでsetState()はなるべく使いたくない
- provider定義やviewmodelのメソッドはriverpodアノテーションをつけて定義してriverpod_generatorで作成
- go_routerを用いた画面遷移
- Freezedを用いたモデル作成
- ローカルDBはIsarを使用

# 5. コードスタイル
- 可能な限り分割代入を活用
- 関数名と変数名は camel_case、クラス名は PascalCase で統一

# 6. フォルダ構成
- 以下のフォルダ構成をベースに作成し、必要であれば下記に記載していない新しいフォルダを随時作成する
  lib/
  ├── main.dart
  ├── router.dart
  ├── core/
  │   ├── utils/ 汎用的なユーティリティ関数やエクステンション
  │   └── constants/ 定数やアプリケーション設定
  ├── data/
  │   ├── models/ Freezedを用いて定義したデータモデル
  │   ├── repositories/ データソース（APIやローカルデータベース）へのアクセスを提供
  │   └── services/ 外部サービスとの通信ロジック
  ├── domain/
  │   ├── use_cases/　ビジネスロジックをカプセル化したユースケース
  │   └── repositories/ ドメイン層で使用するリポジトリのインターフェース
  ├── presentation/
  │   ├── view_models/ 各viewに対するview_model(Riverpodでの状態管理)
  │   ├── views/ 各画面のビュー
  │   └── widgets/
  │       ├── common/ 複数の画面で共有されるウィジェット
  │       └── feature/ 特定の機能に関連するウィジェット